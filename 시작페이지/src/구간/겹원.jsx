/* ═══════════════════════════════════════════════════════
   겹겹의 큰 원 — 피그마 121:2554 · 121:2377

   [왜 그림을 버리고 그리나]
   ① **정원은 돌아도 안 보인다.** 원본은 완전한 동심원 세 개라, 아무리
      돌려도 화면은 똑같다. 돌고 있다는 걸 보이게 하려면 기울여야 한다.
      기울인 원은 타원이 되고, 타원이 돌면 그 움직임이 눈에 들어온다.
      게다가 기울기를 서로 다르게 주면 고리들이 앞뒤로 **교차**한다.
   ② 원본 SVG 두 장이 **1.4MB + 1.1MB** 였다. 선 세 개를 그리는 데
      2.5MB 를 쓸 이유가 없다. CSS 로 그리면 0 바이트다.
   ③ 두께를 고리마다 따로 줄 수 있다. 바깥일수록 얇아야 멀리 있어 보인다.

   [가볍게]
   도는 건 transform 뿐이라 배치를 건드리지 않는다. 장식이라 클릭도 막는다.
   ═══════════════════════════════════════════════════════ */

export default function 겹원({ 칸, 무늬 = "가", 이름 }) {
  return (
    <div
      style={{ ...칸, perspective: "1500px", pointerEvents: "none" }}
      data-node-id={이름}
      aria-hidden="true"
    >
      <div className={`겹원판 겹원-${무늬}`}>
        {/* 바깥 — 가장 얇고 가장 많이 누워 있다 */}
        <div className="고리 고리-바깥">
          <div className="고리훑기" />
        </div>
        {/* 가운데 — 반대로 기울어 바깥 고리와 두 곳에서 엇갈린다 */}
        <div className="고리 고리-중간">
          <div className="고리훑기 고리훑기-느림" />
        </div>
        {/* 안 — 가장 두껍고 가장 서 있다 */}
        <div className="고리 고리-안" />
      </div>
    </div>
  );
}
