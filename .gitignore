# ─────────────────────────────────────────────
#  깃이 추적하지 않을 것들
# ─────────────────────────────────────────────

# 의존성 — package.json 만 있으면 `npm install` 로 그대로 복원된다.
#   289MB 라서 올리면 clone 이 지옥이 되고, 운영체제마다 내용도 다르다.
node_modules/

# 빌드 결과물 — 소스에서 언제든 다시 만들어진다
dist/
dist-*/

# 배포 도구가 만드는 것
.vercel/

# 에디터 · 운영체제 찌꺼기
.DS_Store
.vscode/
.idea/
*.log
npm-debug.log*

# 손으로 만든 백업 — 이제 깃이 대신한다.
#   (당장 지우지는 않는다. 내 컴퓨터에는 남아 있고, 깃에만 안 올라간다)
src/*.backup*.jsx
src/*.backup*.css
_to_delete/

# 파이썬 바이트코드 캐시 — Blender 도구를 돌리면 생긴다
__pycache__/
*.pyc
